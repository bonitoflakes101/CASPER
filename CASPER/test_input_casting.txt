birth {

@main_casper() {
    display "Enter a number for boolean conversion (0 for Night, any other number for Day):\n";
    bln $bool_val = input();
    display "Boolean value: ", $bool_val, "\n";
    
    display "Enter a number for integer truncation (decimal will be truncated):\n";
    int $int_val = input();
    display "Integer value: ", $int_val, "\n";
    
    display "Enter an integer for float conversion (will add .0):\n";
    flt $float_val = input();
    display "Float value: ", $float_val, "\n";
    
    display "Enter Day or Night for integer conversion:\n";
    int $day_night_val = input();
    display "Day/Night as integer: ", $day_night_val, "\n";
}

}
ghost 